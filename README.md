# TaskMaster AI 🚀

Интеллектуальный кроссплатформенный менеджер задач для визуализации недельной нагрузки, повышения продуктивности и предотвращения выгорания.

---

### 🔍 **Общее описание**

-   **Визуализируй** свою неделю с помощью удобного календаря.
-   **Отслеживай** уровень загруженности с помощью уникального "Индикатора выгорания".
-   **Управляй** задачами голосом или текстом через встроенного ИИ-ассистента.
-   **Планируй** эффективнее, задавая задачам важность, сложность и дедлайны.
-   **Синхронизируй** данные между веб-версией, iOS и Android устройствами.

---

### 🧰 **Технологический стек**

| Слой | Технологии |
| :--- | :--- |
| **Frontend** | React Native, Expo, TypeScript, Expo Router |
| **Backend (BaaS)**| Supabase, PostgreSQL, Deno (Edge Functions) |
| **AI** | Google Gemini API (Function Calling) |
| **Инфраструктура**| REST API, RLS (Row Level Security) |

---

### 📁 **Структура проекта**

```
├── backend/
│   └── supabase/
│       ├── migrations/
│       │   └── initial_schema.sql  # SQL-схема для развертывания БД
│       └── functions/
│           └── ask-gemini/
│               └── index.ts        # Код серверной функции для ИИ
├── docs/
│   ├── images/                     
│   └── README.md                   
├── frontend/
│   ├── app/                        # Экраны и навигация (Expo Router)
│   ├── components/                 # Переиспользуемые UI-компоненты
│   ├── contexts/                   # Глобальное управление состоянием (React Context)
│   ├── hooks/                      # Пользовательские хуки
│   ├── lib/                        # Настройка Supabase-клиента и Auth
│   ├── services/                   # Логика взаимодействия с API
│   ├── types/                      # TypeScript-типы и интерфейсы
│   ├── .env.example                
│   └── package.json                
└── README.md    
```

📦 Как запустить проект
### 1. Клонировать репозиторий
```Bash
git clone https://github.com/nicitos/TO-DO-WEB.git
cd TO-DO-WEB
```
### 2. Настройка бэкенда (Supabase)
- Создайте новый проект на Supabase.
- Перейдите в SQL Editor и выполните весь скрипт из файла backend/supabase/migrations/initial_schema.sql.
- Перейдите в Edge Functions, создайте функцию ask-gemini и вставьте в нее код из backend/supabase/functions/ask-gemini/index.ts.
- Создайте API-ключ в Google AI Studio.
- Перейдите в Settings -> Vault вашего проекта Supabase, создайте секрет с именем GEMINI_API_KEY и вставьте в него ваш ключ.

### 3. Настройка фронтенда
Перейдите в папку с приложением
```Bash
cd frontend
```

Установите зависимости
```Bash
npm install
npx expo install @react-native-community/datetimepicker
```
Создайте файл .env и заполните его вашими ключами из Supabase
```
cp .env.example .env 
```
(Затем откройте .env и вставьте реальные ключи)

### 4. Запуск
```Bash
# Убедитесь, что вы находитесь в папке frontend
npx expo start -c
```
- Веб-версия будет доступна по http://localhost:8081 (или другому порту).
- Мобильная версия запускается через приложение Expo Go путем сканирования QR-кода.


### 🛠️ Основные функциональные модули
- **Auth:** Регистрация, вход, выход, управление сессиями.
- **Tasks:** CRUD-операции для задач (создание, чтение, обновление, удаление).
- **Weekly View:** Визуализация задач на недельном календаре.
- **Burnout Indicator:** Расчет и отображение дневной нагрузки на основе сложности задач.
- **AI Assistant:** Обработка команд на естественном языке, вызов функций для создания/изменения задач.
- **Profile & Stats:** Отображение статистики пользователя.


### 📑 API (Серверные функции)
В проекте используется две основные категории взаимодействия с бэкендом:
- `Прямые запросы к таблицам через Supabase REST API (например, select, insert, update для задач).`
- `Вызов серверных RPC-функций для более сложной логики.`
- `POST /rest/v1/rpc/get_burnout_scores_for_week` — получает массив с показателями выгорания на неделю.
- `POST /functions/v1/ask-gemini` — основной эндпоинт для взаимодействия с ИИ-ассистентом.


### 🔥 Ключевая особенность: Индикатор выгорания
- **Расчет:** Значение индикатора — это SUM(complexity) всех незавершенных задач на конкретный день.
- **Логика:** Расчет происходит "на лету" на стороне PostgreSQL с помощью RPC-функции get_burnout_scores_for_week, что гарантирует актуальность данных при каждом запросе.
- **Визуализация:** Отображается в виде прогресс-бара с цветовой кодировкой:
- Relaxed (Зеленый)
- Balanced (Желтый)
- Busy (Оранжевый)
- Overloaded (Красный)


### 🧩 Особенности архитектуры
- **Монорепозиторий:** Четкое разделение на frontend, backend и docs.
- **BaaS (Backend-as-a-Service):** Максимальное использование готовых сервисов Supabase для ускорения разработки.
- **Serverless:** Использование Edge Functions для серверной логики без необходимости управлять сервером.
- **Безопасность:** Row Level Security (RLS) в PostgreSQL гарантирует, что пользователи видят только свои данные.
- **Кроссплатформенность:** Единая кодовая база на React Native & Expo для iOS, Android и Web.

### 🚀 Быстрый старт: Команды
# Запустить проект для разработки (из папки frontend)
```Bash
npm run start
```
# Запустить веб-версию (из папки frontend)
```Bash
npm run web
```
# Запустить iOS симулятор (из папки frontend)
```Bash
npm run ios
```
# Запустить Android эмулятор (из папки frontend)
```Bash
npm run android
```
(Эти команды являются стандартными для npx expo start и могут быть добавлены в package.json)

## Структура данных
Задача (Task)
```TypeScript
interface Task {
  id: string;
  user_id: string;
  title: string;
  description?: string | null;
  deadline?: string | null;
  importance: 'low' | 'medium' | 'high';
  complexity: 1 | 2 | 3 | 4 | 5;
  scheduled_date?: string | null;
  completed: boolean;
}
```
## Создание задачи (CreateTaskData)
```TypeScript
interface CreateTaskData {
  title: string;
  description?: string | null;
  deadline?: string | null;
  importance: 'low' | 'medium' | 'high';
  complexity: 1 | 2 | 3 | 4 | 5;
  scheduled_date?: string | null;
}
```
### 🔗 Полезные ссылки
Документация Expo
Документация Supabase
Документация Google AI
React Native
